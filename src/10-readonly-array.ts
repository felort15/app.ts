const numbers : ReadonlyArray<number> =[1,2,2,22]

// numbers.push(9);
// numbers.pop();
// numbers.unshift(1);
numbers.filter((number)=>{});
 numbers.reduce((acc, number)=>0);
 numbers.map(()=>0);
